'use strict';

/*

  Data In: User input from prompt
  Data Out: Alert message with information about the input

  Test Cases: 
  - When user cancels the prompt (input === null)
  - When user inputs an empty string (input.length === 0)
  - When user inputs a non-empty string

*/

/* ---   Get User Input   --- */

// input <- prompt('do whatever you want')

/* ---   Prepare Message   --- */

// Initialize message variable
// message <- 'your input is '

// Check if the user canceled the prompt
// IF: input === null
/* ---   Handle Cancel   --- */
//   message <- message + 'null'
// ELSE:
/* ---   Handle Non-Cancel   --- */
//   maybeNot <- ''
//   Check if input is not empty
//   IF: input.length !== 0
/* ---   Handle Non-Empty Input   --- */
//     maybeNot <- 'not '
//   :END IF
//   message <- message + maybeNot + 'empty'
// :END IF

/* ---   Display Message   --- */

// alert(message)
