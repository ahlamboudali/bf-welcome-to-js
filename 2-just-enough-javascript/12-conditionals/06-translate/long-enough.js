'use strict';
/*


  Data In:

  Data Out:

  Test Cases:

*/

/* ---   ?   --- */

// Prompt the user to enter anything longer than 5 characters
// input <- prompt('enter anything longer than 5 characters')

// Check if the user didn't cancel the prompt
// IF: input !== null
//   Check if input is shorter than 5 characters
//   IF: input.length < 5
/* ---   Handle Short Input   --- */
//     message <- 'too short'
//   Check if input is longer than 5 characters
//   ELSE: IF: input.length > 5
/* ---   Handle Long Input   --- */
//     message <- 'long enough'
//   Otherwise, input must be exactly 5 characters
//   ELSE:
/* ---   Handle Exactly 5 Characters   --- */
//     message <- 'exactly 5!'
//   :END IF
// ELSE:
/* ---   Handle Cancel   --- */
//   message <- 'you canceled :('
// :END IF

/* ---   Display Message   --- */

// Display the message in an alert dialog
// alert(message)
